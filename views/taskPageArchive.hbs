<main>

    {{#if completed}}

    <h4 class="text-center">{{completed.length}} завършени задачи</h4>

    <div class="cards">

        {{#each completed}}
        {{> task}}
        {{/each}}

    </div>

    {{else}}
        <h3>Няма завършени задачи...</h4class=>
    {{/if}}

</main>