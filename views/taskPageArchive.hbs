<main>

    {{#if completed}}

    <h3 class="text-center">{{completed.length}} завършени задачи</h3>

    <div class="cards">

        {{#each completed}}
        {{> task}}
        {{/each}}

    </div>

    {{else}}
        <h3>Няма завършени задачи...</h4class=>
    {{/if}}

</main>