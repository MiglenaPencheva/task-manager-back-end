<section>

    {{#if formatedMyTasks}}

        {{#if isOne}}
            <h3>1 моя задача</h3>
        {{else}}
            <h3>{{formatedMyTasks.length}} мои задачи</h3>   
        {{/if}}


    <div>
        {{#each formatedMyTasks}}
            {{> task}}
        {{/each}}
    </div>


    {{else}}
        <h3>Няма мои задачи...</h3>
    {{/if}}

</section>