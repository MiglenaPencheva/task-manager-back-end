<main>

    {{#if myTasks}}

    <h4 class="text-center">{{myTasks.length}} задачи, създадени от {{user.username}}</h4>

    <div class="cards">

        {{#each myTasks}}
        {{> task}}
        {{/each}}

    </div>


    {{else}}
        <h4>Няма мои задачи...</h4>
    {{/if}}

</main>