<main>

    {{#if myTasks}}

    <h3 class="text-center">{{myTasks.length}} задачи, създадени от {{user.username}}</h3>

    <div class="cards">

        {{#each myTasks}}
        {{> task}}
        {{/each}}

    </div>


    {{else}}
        <h3>Няма мои задачи...</h3>
    {{/if}}

</main>